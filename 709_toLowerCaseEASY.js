/**
 * @param {string} s
 * @return {string}
 */
var toLowerCase = function(s) {
    
    if(s.toLowerCase() || s.toUpperCase() || s[0].toLowerCase())      return s.toLowerCase();
    
};

/*

Input: s = "Hello"
Output: "hello"
Example 2:

Input: s = "here"
Output: "here"
Example 3:

Input: s = "LOVELY"
Output: "lovely"

*/
